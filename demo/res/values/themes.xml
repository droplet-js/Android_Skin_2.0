<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="Theme.Shell" parent="@style/Theme.Device">

        <!--  -->
        <item name="android:windowBackground">@drawable/skin_bg</item>
        <!-- 弹出或者进入时的动画效果 -->
        <item name="android:windowAnimationStyle">@style/Animation.Activity.Shell</item>
    </style>

    <style name="Theme.Home" parent="@style/Theme.Device">

        <!--  -->
        <!-- 弹出或者进入时的动画效果 -->
        <item name="android:windowAnimationStyle">@style/Animation.Activity.Home</item>
    </style>

    <style name="Theme.Slide" parent="@style/Theme.Device">

        <!--  -->
        <!-- 弹出或者进入时的动画效果 -->
        <item name="android:windowAnimationStyle">@style/Animation.Activity.Slide</item>
    </style>

</resources>